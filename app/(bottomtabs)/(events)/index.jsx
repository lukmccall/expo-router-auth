import { Linking, SafeAreaView, Settings, Text, View } from 'react-native';

export default function Home() {
  return (
    <SafeAreaView>
      <Text style={{fontSize: 36}}>Home</Text>
    </SafeAreaView>
  );
}